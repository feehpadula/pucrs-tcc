import "./Select.scss";

const Button = (props) => {
  return (
    <div className="select-container">
      <select name="" id="">
        <option value="valor1">Brasil</option>
      </select>
    </div>
  );
};

export default Button;
